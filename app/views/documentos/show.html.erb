<div class="container">
  <div class="card mt-5">
    <div class="card-header">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="">Documento</a></li>
        <li class="breadcrumb-item active">Características</li>
      </ol>

      <h5 class="text">Documento <%= @documento.id%> </h5>
    </div>
    <div class="probar">
      <%= render @documento %>

      <div>
        <div>
        <% if current_user.role =='administrador'%>
          <%= button_to 'Eliminar', { :action => :destroy, :id => @documento.id}, :data => { :confirm => 'Esta seguro?' }, :method => :delete,  class: "btn btn-danger btn-eliminar"%>

          <%= link_to 'Regresar', url_for(:back),  class: "btn btn-success btn-xs round btn-regresar"%>
          <%end %>
          
        </div>
        <%= link_to "Regresar a documentos", documentos_path %>
       
        <br>
        <br>
        </br>
      </div>